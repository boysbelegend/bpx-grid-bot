# Dashboard Backend Configuration
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:5173

# Path to main bot directory
BOT_ROOT_PATH=../../

# Trading Engine WebSocket (if running separately)
ENGINE_WS_PORT=3002
